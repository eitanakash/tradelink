{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm install --no-frozen-lockfile && pnpm --filter web build"
  },
  "deploy": {
    "startCommand": "node apps/web/serve.js",
    "restartPolicyType": "ON_FAILURE"
  }
}

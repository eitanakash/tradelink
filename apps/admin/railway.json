{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm install --no-frozen-lockfile && pnpm --filter @tradelink/admin build"
  },
  "deploy": {
    "startCommand": "node apps/admin/serve.cjs",
    "restartPolicyType": "ON_FAILURE"
  }
}
